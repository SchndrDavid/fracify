# Host port the container is published on. Change it only if it clashes with
# another service on the same host.
FRACIFY_PORT=8105

# UID/GID the container runs as. Match them to the host user if you care; the
# app writes nothing, so the defaults are fine.
FRACIFY_UID=1000
FRACIFY_GID=1000
